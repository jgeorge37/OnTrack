<h1>OnTrack Project 3: Web Scraping</h1>
<h4>Last updated: <%= "current time" %></h4>
<table>
  <thead>
    <tr>
      <th>Course</th>
      <th>Class #</th>
      <th>Day(s)</th>
      <th>Time(s)</th>
      <th>Location(s)</th>
      <th>Enrollment</th>
      <th>Waitlist</th>
      <th>Instructor</th>
    </tr>
  </thead>

  <tbody>
    <%= @classes.each do |class| %>
      <tr>
        <td><%= %></td> <!-- Course (ex: CSE 3901) -->
        <td><%= %></td> <!-- Class number (ex: 6845) -->
        <td><%= %></td> <!-- Days (MTWRF) -->
        <td><%= %></td> <!-- Times (ex: 0800A) -->
        <td><%= %></td> <!-- Locations (ex: DL0357) -->
        <td><%= %></td> <!-- Enrollment (ex: 35/40) -->
        <td><%= %></td> <!-- Waitlist (ex: 2) -->
        <td><%= %></td> <!-- Instructor (ex: P.Bucci) -->
      </tr>
  </tbody>
</table>
<br>
